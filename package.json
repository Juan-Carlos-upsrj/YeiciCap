
{
  "name": "yeicicap-hub",
  "version": "1.6.0",
  "main": "main.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "dist:backend": "python -m PyInstaller --onefile --name hub_core backend/main.py",
    "dist:ui": "electron-builder build --win --publish never",
    "dist": "npm run build && npm run dist:backend && npm run dist:ui"
  },
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "lucide-react": "^0.562.0"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-builder": "^24.13.3",
    "vite": "^5.0.0"
  }
}
